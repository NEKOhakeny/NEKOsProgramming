#include<iostream>
#include<cmath>
#include<cstring>
#include<cstdlib>
#include<string>
#include<cstdio>
#define rep(a,b,c) for(int a = b ; a < c ; a++)
using namespace std;
int  stack[100];
int main(void){
    char array[200][10];
    char literal[300];
    int word = 0;
    fgets(literal,200,stdin);
    int j = 0;
    int k = 0;
    for(int i = 0; ; i++){
        if(literal[i] == ' '){
            k = 0;
            j++;
            continue;
        }
        array[j][k] = literal[i];
        k++;
        if(literal[i] == '¥0')break;
    }
    rep(i,0,j+1)cout<<array[i]<<endl;
    return 0;

    
}
