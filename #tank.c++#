#include<iostream>
#include<string.h>
using namespace std;
int main(void){
  while(1){
    int n;
    cin>>n>>endl;
    if(n == 0)break;
    char word[n][7];
    for(int i = 0 ; i < n; i++){
      cin>>word[i];
    }
    int sum == 0;
    for(int i = 1 ; i <=5 ; i++){
      sum += strlen(word) 
	if(i = 1 ||i = 3){
	  if(
    